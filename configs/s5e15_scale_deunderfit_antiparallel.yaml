experiment: S5E15_scale_deunderfit_and_antiparallel_joint_refinement
report_language: zh
scene: scene01
eval_seq: seq03
timestamps: external_baselines/dataset/scene01_seq03/timestamps.txt
groundtruth: external_baselines/dataset/scene01_seq03/groundtruth_tum.txt
s5e12_feature_dir: external_baselines/results/s5e12_feature_gate
s5e14_dir: external_baselines/results/s5e14_traceable_dense
output_dir: external_baselines/results/s5e15_traceable_dense
candidate_checkpoint_json: checkpoints/S5E15_scale_deunderfit_antiparallel_candidate.json
training_status_json: checkpoints/S5E15_scale_deunderfit_antiparallel_candidate/training_status.json
report_md: reports/s5e15_scale_deunderfit_antiparallel_report.md
comparison_md: reports/s5e15_s5e14_s5e13_s5e9_orbslam3_comparison.md

refinement:
  train_prior_scale_factor: 2.8
  scale_factor_source: train_split_prior
  scale_factor_min: 1.0
  scale_factor_max: 5.0
  anti_parallel_risk_angle_dispersion_min: 70.0
  anti_parallel_risk_low_confidence_max: 0.45
  anti_parallel_guard_fallback: S5E13
  high_confidence_min: 0.75

losses:
  scale_deunderfit_prior: true
  anti_parallel_guard: true
  robust_tmag_log: true
  signed_tdir_confidence_gate: true

caveats:
  - S5E15 是 experimental candidate，不替代 official S5 locked result。
  - S5 locked metrics/policy unchanged。
  - 没有使用 eval GT 做 scale/sign calibration。
  - oracle diagnostic 如有，不作为 candidate 指标。
  - 不修改 final manifest、train/test split、official evaluator。
