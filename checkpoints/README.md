# Checkpoints

This directory is treated as local checkpoint storage.

- do not commit `.pt`, `.pth`, or `.ckpt` files in cleanup branches
- current protected local checkpoint path:
  `checkpoints/FINAL360I_struct360b_final/seed0/best_val.pt`
- retained variant local checkpoint path if present:
  `checkpoints/SEQ360B_lightweight_scale_smoothing/best_val.pt`

